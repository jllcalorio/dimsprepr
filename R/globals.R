#' Global variables to avoid R CMD check notes
#' @noRd
globalVariables(c(
  "injection", "value", "group", "batch",  # plot_beforeafter
  ".data"                                    # plot_dist_beforeafter
))
